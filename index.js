
var isFunction = require('./fn/is-function')
var param = require('./fn/param')
var emitter = require('./fn/emitter')

module.exports = {
  isFunction: isFunction,
  param: param,
  emitter: emitter
}
